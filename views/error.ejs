<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Error - <%= status %> | YelpCamp</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    body {
      background: linear-gradient(to right, #fdfbfb, #ebedee);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      font-family: 'Segoe UI', sans-serif;
    }

    main {
      flex-grow: 1;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .error-container {
      text-align: center;
      padding: 40px 20px;
      max-width: 600px;
      margin: auto;
      background-color: white;
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .error-code {
      font-size: 6rem;
      font-weight: 700;
      color: #dc3545;
    }

    .error-title {
      font-size: 2rem;
      font-weight: 600;
      color: #343a40;
      margin-bottom: 15px;
    }

    .error-message {
      font-size: 1.1rem;
      color: #6c757d;
      margin-bottom: 25px;
    }

    .error-details {
      font-size: 0.95rem;
      color: #adb5bd;
      margin-bottom: 20px;
    }

    .btn-home {
      padding: 10px 20px;
      font-size: 1rem;
    }

    .campfire-img {
      width: 100px;
      margin-bottom: 20px;
    }
  </style>
</head>

<body>
  <!-- Navbar Include -->
  <%- include('./partials/navbar.ejs') %>

  <!-- Main Content -->
  <main style="height:600px" class="mt-3">
    <div class="container error-container">
      <img src="/images/campfire.png" alt="Campfire Icon" class="campfire-img">
      <div class="error-code"><%= status || 500 %></div>
      <div class="error-title">Oops! Something went wrong.</div>
      <div class="error-message"><%= message || "Unexpected error occurred. Please try again later." %></div>

      <% if (process.env.NOE_ENV!=='production' && error && error.stack) { %>
        <div class="error-details">
          <strong>Details for developers:</strong><br />
          <pre><%= error.stack %></pre>
        </div>
      <% } %>

      <a href="/campgrounds" class="btn btn-dark btn-home">← Back to Campgrounds</a>
    </div>
  </main>

  <!-- Footer Include -->
  <%- include('./partials/footer.ejs') %>
</body>
</html>
